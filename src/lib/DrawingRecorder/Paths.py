import os
from os.path import abspath, join
import sys

ROOT    = abspath(join(__file__, '../../..'))
ICONS   = join(ROOT, "icons")
INTL    = join(ROOT, "intl")
RX      = join(ROOT, "rx")
UI      = join(ROOT, "ui")
